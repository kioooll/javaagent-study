# Multi-Agent 与编排

返回入口：[[00-JavaAgent-首页]]

相关原始资料：[[progress/java-agent-tracker]] ｜ [[sessions/2026-03-19/session-notes]] ｜ [[sessions/2026-03-20/session-notes]] ｜ [[sessions/2026-03-21/session-notes]]

---

## 一句话理解
Multi-Agent 不是简单地把 Agent 变多，而是把复杂任务拆成多个有边界的角色，再由编排层统一控制依赖、并发和失败路径。

## 核心概念
- **Orchestrator / SubAgent 分工**
  - Orchestrator 负责识别任务、拆解计划、调度执行和汇总结果。
  - SubAgent 负责处理自己那一小块专业任务，避免所有职责都塞进一个 Agent。

- **任务拆解与并行执行**
  - 没有依赖关系的任务可以并行，提高整体吞吐。
  - 有上下游依赖的任务必须串行，否则下游拿不到完整上下文。

- **CompletableFuture 适合表达异步编排链路**
  - `supplyAsync()` 适合启动异步任务。
  - `allOf()` 适合等待多个依赖完成。
  - `thenCompose()` 适合把“上一步结果”继续接到下一步异步流程里。

- **Agent 间通信要防死循环**
  - 子 Agent 互调时，需要有调用链追踪和最大深度控制。
  - 否则很容易出现 Data Agent → RAG Agent → Data Agent 这样的循环依赖。

- **DAG 错误传播要提前设计**
  - 某个分支失败后，系统是 fail-fast、部分成功，还是降级继续，都要明确。
  - 在复杂链路里，错误如果只在底层抛出、不向上游解释，最终结果会很难读也很难维护。

- **并发调优不是越多越好**
  - LLM 调用通常是 IO 密集型，更关注线程池配置、队列上限和资源边界。
  - 并发前要先想清楚上下文传递、超时、拒绝策略和下游承载能力。

## 关键工程结论
- 多 Agent 的价值不在“多”本身，而在于**职责切分和上下游边界清晰**。
- 编排层必须明确错误传播策略，否则复杂链路会很快变得不可控。
- 并发执行提升吞吐的前提，是上下文、资源池和失败路径都设计清楚。

## 继续阅读
- [[01-学习主线]]
- [[progress/java-agent-tracker]]

# RAG 与检索增强

返回入口：[[00-JavaAgent-首页]]

相关原始资料：[[progress/rag-best-practices]] ｜ [[progress/java-agent-tracker]]

---

## 一句话理解
RAG 是先检索、再生成：先把最相关的知识取回来，再把这些上下文交给模型回答问题。

## 核心概念
- **切块大小与 overlap 是第一层效果开关**
  - 切块太小，容易丢失上下文。
  - 切块太大，语义会被稀释，token 成本也会上升。
  - overlap 用来减少关键信息被切断的概率。

- **不同数据形态不能一刀切**
  - FAQ 更适合按一问一答切分。
  - 表格常常需要转成更易检索的行式文本或结构化片段。
  - 代码文档更适合按函数、类或注释块组织。

- **BM25 与向量检索是互补关系**
  - BM25 擅长专有名词和精确关键词。
  - 向量检索擅长语义相似和同义表达。
  - 真实场景里，二者结合通常比单一路线更稳。

- **RRF 用来融合多路检索结果**
  - 它不是重新理解内容，而是把多个结果列表按排名融合。
  - 在关键词和语义召回都表现不错的文档，通常会被融合后排得更靠前。

- **RAG 评估不能只看“答得像不像”**
  - 至少要看检索准确率、回答忠实度、回答相关性。
  - 如果检索错了，生成层再强也很难稳定补救。

- **Prompt 注入防御属于检索链路的一部分**
  - 外部文档可能携带恶意指令，不能把检索内容当成天然可信输入。
  - 输入过滤、权限控制、输出校验需要一起做。

## 关键工程结论
- RAG 的效果很多时候先输在**数据组织和切块策略**，而不是输在模型上。
- 纯向量检索和纯关键词检索都不够稳，混合检索更接近生产方案。
- Prompt 注入防御不是单一规则，而是输入、权限和输出验证的组合问题。

## 继续阅读
- [[11-Memory-与上下文]]
- [[13-Multi-Agent-与编排]]

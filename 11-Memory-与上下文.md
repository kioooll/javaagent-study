# Memory 与上下文

返回入口：[[00-JavaAgent-首页]]

相关原始资料：[[progress/memory-system-design]] ｜ [[progress/java-agent-tracker]]

---

## 一句话理解
Memory 是让 Agent 在多轮交互里保留、压缩、检索历史信息的上下文层，直接决定系统能不能稳定连续地“记住重点”。

## 核心概念
- **短期记忆 vs 长期记忆**
  - 短期记忆保存最近几轮对话，服务当前任务。
  - 长期记忆保存用户画像、摘要或可检索知识，服务跨轮次和跨会话场景。

- **生产环境的短期记忆不能只放单机内存**
  - 多实例部署下，请求会被负载均衡到不同机器。
  - 如果记忆只在单机内存里，重启会丢、扩容后也无法共享。

- **Redis 是生产级短期记忆底座**
  - 常见做法是按 `session_id` 存消息列表，并设置 TTL。
  - 这样任意实例都能读到同一段会话历史，同时能自动过期清理。

- **MessageWindow / TokenWindow / 摘要策略各有侧重**
  - MessageWindow：按消息条数裁剪，简单直接。
  - TokenWindow：按 token 数裁剪，更适合成本控制。
  - 摘要策略：不是直接丢弃旧信息，而是把它压缩成更短的高价值记忆。

- **Context 管理是多轮 Agent 的运行基础**
  - 它不只是“拼 prompt”，还承担历史承接、状态传递、工具结果回灌和成本控制。
  - 做不好上下文管理，多轮系统很容易失忆、漂移或变贵。

## 关键工程结论
- 多实例环境下，短期记忆首先是**分布式状态管理问题**，而不只是 Prompt 拼接问题。
- Redis 更像生产级短期记忆底座，摘要和语义检索是进一步的容量优化手段。
- Memory 的目标不是“永远都记住”，而是“在成本约束下记住最有用的信息”。

## 继续阅读
- [[12-RAG-与检索增强]]
- [[13-Multi-Agent-与编排]]
